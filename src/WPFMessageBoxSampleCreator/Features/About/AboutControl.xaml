<UserControl
    x:Class="WPFMessageBoxSampleCreator.Features.About.AboutControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WPFMessageBoxSampleCreator.Features.About"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance local:AboutViewModel,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  アイコン  -->
        <Image
            Grid.Row="0"
            Grid.Column="0"
            Width="48"
            Height="48"
            Source="Icon.ico" />

        <!--  アプリ名 & バージョン  -->
        <StackPanel
            Grid.Row="0"
            Grid.Column="1"
            VerticalAlignment="Center">
            <TextBlock
                FontSize="20"
                FontWeight="Bold"
                Text="{Binding AppTitle}" />
            <TextBlock
                FontSize="14"
                Foreground="#666"
                Text="{Binding AppVersion}" />
        </StackPanel>

        <!--  リンク類  -->
        <StackPanel
            Grid.Row="1"
            Grid.Column="1"
            Margin="0,10,0,0">
            <TextBlock>
                <Hyperlink NavigateUri="https://github.com/yourrepo" RequestNavigate="Hyperlink_RequestNavigate">
                    GitHub
                </Hyperlink>
            </TextBlock>

            <TextBlock>
                <Hyperlink NavigateUri="https://yourwebsite.com" RequestNavigate="Hyperlink_RequestNavigate">
                    Website
                </Hyperlink>
            </TextBlock>

            <TextBlock>
                <Hyperlink NavigateUri="mailto:contact@example.com" RequestNavigate="Hyperlink_RequestNavigate">
                    Contact
                </Hyperlink>
            </TextBlock>

        </StackPanel>

        <!--  ライセンス情報（スクロール）  -->
        <Border
            Grid.Row="2"
            Grid.ColumnSpan="2"
            Margin="0,20,0,20"
            Padding="10"
            BorderBrush="#DDD"
            BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock Text="{Binding LicenseText}" TextWrapping="Wrap" />
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
